import { pipe, pipeWith } from './pipe';
import { compose, composeWith } from './compose';
import { tap } from './tap';
import { curry } from './curry';

export {
  pipe,
  pipeWith,
  compose,
  composeWith,
  tap,
  curry
}